# Secure LAN Chat

Это Python-приложение для безопасного общения и обмена файлами внутри локальной сети (LAN). Приложение использует **UDP Broadcast** для автоматического поиска собеседников и **TCP** с шифрованием для передачи сообщений и файлов.

## Возможности

*   **Автоматическое обнаружение:** Не нужно вводить IP-адреса. Приложение само находит других пользователей в сети.
*   **Сквозное шифрование:** Все сообщения и файлы шифруются симметричным ключом (Fernet/AES) перед отправкой.
*   **Передача файлов:** Возможность отправлять любые файлы (изображения, архивы и т.д.) с подтверждением приема от собеседника.
*   **Работа с большими данными:** Поддержка отправки длинных сообщений и больших файлов благодаря поблочной передаче.
*   **Графический интерфейс:** Удобный GUI на базе `tkinter`.

## Требования

*   Python 3.8+
*   Библиотека `cryptography`

## Установка и Запуск

1.  **Склонируйте репозиторий или скачайте файлы:**
    ```bash
    git clone https://github.com/your-username/lan-chat.git
    cd lan-chat
    ```

2.  **Создайте виртуальное окружение (рекомендуется):**
    ```bash
    python -m venv .venv
    # Windows:
    .venv\Scripts\activate
    # Linux/Mac:
    source .venv/bin/activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install cryptography
    ```

4.  **Запустите приложение:**
    ```bash
    python main.py
    ```

## Как пользоваться

1.  **Вход:** При запуске введите свой никнейм и нажмите "Подключиться".
2.  **Список пользователей:** В главном окне вы увидите список пользователей онлайн. Если список пуст, убедитесь, что другие пользователи подключены к той же Wi-Fi/LAN сети.
3.  **Начало чата:** Дважды кликните по пользователю в списке или выделите его и нажмите кнопку "Чат".
4.  **Обмен файлами:**
    *   В окне чата нажмите кнопку **"📎 Файл"**.
    *   Выберите файл на диске.
    *   Получатель увидит запрос "Вам отправляют файл... Принять?".
    *   После согласия начнется загрузка.

## Технические детали

### Структура проекта
*   `main.py` — Точка входа.
*   `gui/` — Графический интерфейс (App, ChatWindow).
*   `network/` — Сетевая логика:
    *   `broadcast_discovery.py` — Поиск пользователей (UDP порт 5007).
    *   `tcp_srv.py` / `tcp_client.py` — Транспортный уровень.
    *   `protocol.py` — Упаковка данных, шифрование и работа с файлами.
*   `utils/constans.py` — Настройки портов и **ключ шифрования**.

### Протокол передачи
Для решения проблемы склеивания пакетов TCP используется префикс длины:
```
[Length (4 bytes)] + [Encrypted JSON Payload]
```
1.  **Length:** 4 байта (big-endian), указывающие размер зашифрованных данных.
2.  **Payload:** JSON-структура, зашифрованная Fernet.

### Безопасность
Используется библиотека `cryptography.fernet`.
> **Важно:** Ключ шифрования (`ENCRYPTION_KEY`) жестко задан в файле `utils/constans.py` для упрощения демонстрации. Для реального использования рекомендуется генерировать новый ключ и обмениваться им безопасным способом.

## Известные нюансы
*   Фаервол (Windows Defender / UFW) может блокировать подключения. При первом запуске **разрешите доступ** для Python.
*   Для работы автообнаружения устройства должны находиться в одной подсети.

---
**Автор:** Ваше Имя
**Лицензия:** MIT